<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
  <h1><%= title %></h1>
  <div class="card-container">
    <% favorites.forEach(favorite => { %>
      <a class="text-decoration-none" href="/games/<%= favorite._id %>">
        <div class="card">
          <img 
            src="<%= favorite.imgUrl %>" 
            alt="<%= favorite.name %>'s avatar"
          >
          <div class="card-body">
            <h1 class="card-title"><%= favorite.name %></h1>
            <form id="delete-form" action="/profiles/<%= game._id %>?_method=DELETE" method="POST">
              <button type="submit">Remove from Favorites</button>
              <form action="/profiles/<%= game._id %>/favorites" method="POST"> 
            </form>
          </div>
        </div>
      </a>
    <% }) %>
  </div>
</main>

<%- include('../partials/footer') %>